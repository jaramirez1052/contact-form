#Usage
```
const setupDatabase = require('contact-db')
setupDabase(config).then(db => {
const { Message } = db

}).catch(err => console.error(err))
```

# Setup Database
```angular2html
npm run setup
or
yarn setup
```